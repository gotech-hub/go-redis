run:
  timeout: 5m
  go: "1.23"
linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
    - gofmt
    - goimports
issues:
  exclude-rules:
    - path: _test\.go
      linters: [errcheck]
